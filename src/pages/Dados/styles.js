import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  resultDado: {
    display: "flex",
    width: "80%",
  },

  viewResult: {
    display: "flex",
    flexDirection: "row",
  },
})

export default styles;