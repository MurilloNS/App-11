import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  inputMult: {
    borderWidth: 1,
    borderColor: "black",
    height: 40,
    width: "75%",
    alignSelf: "center",
    marginTop: 20,
    borderRadius: 10,
    paddingLeft: 5
  }
});

export default styles;